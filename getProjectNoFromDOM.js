export default function getProjectNoFromDOM(taskNo) {
    if (taskNo == 0) {
        const assignedProject = document.getElementById(`0BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 1) {
        const assignedProject = document.getElementById(`1BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 2) {
        const assignedProject = document.getElementById(`2BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 3) {
        const assignedProject = document.getElementById(`3BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 4) {
        const assignedProject = document.getElementById(`4BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 5) {
        const assignedProject = document.getElementById(`5BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 6) {
        const assignedProject = document.getElementById(`6BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 7) {
        const assignedProject = document.getElementById(`7BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 8) {
        const assignedProject = document.getElementById(`8BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 9) {
        const assignedProject = document.getElementById(`9BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 10) {
        const assignedProject = document.getElementById(`10BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 11) {
        const assignedProject = document.getElementById(`11BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 12) {
        const assignedProject = document.getElementById(`12BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 13) {
        const assignedProject = document.getElementById(`13BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 14) {
        const assignedProject = document.getElementById(`14BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 15) {
        const assignedProject = document.getElementById(`15BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 16) {
        const assignedProject = document.getElementById(`16BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 17) {
        const assignedProject = document.getElementById(`17BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 18) {
        const assignedProject = document.getElementById(`18BoxProject`).innerText;
        return assignedProject
    } else if (taskNo == 19) {
        const assignedProject = document.getElementById(`19BoxProject`).innerText;
        return assignedProject
    } 
}